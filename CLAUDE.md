# CLAUDE.md

프로젝트 루트에 두면 Claude Code가 매 세션 자동으로 읽는 상시 지침 파일이다.

---

## 프로젝트

개인 자산관리 앱. 목표 금액·기한을 입력하면 필요 월 저축액을 계산하고,
실제 지출을 추적해 월중에 긴축/여유를 판정한다.

전체 명세는 `docs/SPEC.md` 참조. 작업 시작 전 반드시 읽을 것.

---

## 절대 규칙

### 1. LLM에게 계산을 시키지 않는다
모든 금액·비율 계산은 `src/core/`의 순수 함수가 담당한다.
LLM은 계산된 결과를 자연어로 설명하는 역할만 한다.
LLM 프롬프트에 넘기는 비율은 **미리 퍼센트로 변환**해서 넣는다.

### 2. `src/core/`는 순수 함수만
React, Prisma, fetch, `Date.now()` 직접 호출 금지.
현재 시각이 필요하면 **인자로 주입**받는다 (테스트 가능성 때문).
나중에 React Native로 재사용할 폴더다.

### 3. 금액은 원 단위 정수
부동소수점 금액 금지. 화면 표시 직전에만 `toLocaleString('ko-KR')` 적용.
계산 결과는 `Math.round()`로 정수화.

### 4. 계산 로직에는 반드시 테스트를 함께 작성
`SPEC.md` §5의 검증값을 그대로 테스트 케이스로 쓴다.
계산 함수를 수정하면 테스트부터 확인한다.

### 5. 금융 조언 가드레일
- 특정 종목·상품·코인 추천 금지. 자산군 수준까지만.
- 투자 관련 출력에는 원금 손실 가능성 언급.
- 사용자를 다그치는 문구 금지. 비난 대신 다음 행동 제시.

### 6. API 키
`ANTHROPIC_API_KEY`는 서버 사이드에서만 사용.
클라이언트 번들에 절대 포함되지 않도록 할 것. `NEXT_PUBLIC_` 접두사 금지.

---

## 코드 스타일

- TypeScript strict 모드. `any` 금지.
- 계산 함수에는 수식을 주석으로 남긴다.
- 주석은 "무엇을"이 아니라 **"왜"** 를 적는다.
  예: `// 평균 대신 중앙값 — 이사·명절 같은 1회성 지출에 오염되지 않기 위해`
- 한국어 UI 문자열은 `CATEGORY_LABELS_KO` 같은 상수 맵에 모은다.

---

## 명령어

```bash
npm run dev        # 개발 서버
npm test           # Vitest
npm run test:watch
npx prisma migrate dev
npx prisma studio
```

---

## 작업 방식

- 큰 기능은 한 번에 다 짜지 말고 파일 단위로 끊어서 진행하고 중간에 확인받는다.
- `src/core/` 파일을 수정하면 해당 테스트를 실행해서 통과를 확인한 뒤 다음으로 넘어간다.
- UI보다 계산 엔진을 먼저 완성한다.
